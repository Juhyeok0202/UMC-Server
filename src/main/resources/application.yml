spring:
  datasource:
    url: jdbc:mysql://localhost:3306/umc
    username: root
    password: Wngurdl1!
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size=20:sql: 20
    init:
      mode: never #
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: none
        default_batch_fetch_size: 1000
